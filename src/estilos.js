import { StyleSheet } from 'react-native';

const estilos = StyleSheet.create({
  preencher: {
    flex: 1,
  }
});

export default estilos;

export const cores = {
  roxo: '#A050BE',
  laranja: '#FABE50',
  escuro: '#555555',
  claro: '#fff',
  cinza: '#C7C7C7',
};
